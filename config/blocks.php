<?php

/**
 *  Each block must have a block parser or it cannot be rendered.
 *
 * Array key must correspond to the block handle and the value, the
 * parser namespace.
 *
 * Example:
 * ```
 * return [
 *   'heading' => 'app\blocks\HeadingBlock',
 *   'wysiwyg' => 'app\blocks\TextBlock',
 * ];
 * ```
 *
 */

return [
];
